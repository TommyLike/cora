name: "GitCode · repos list (table)"
service: gitcode
args:
  - repos
  - list
  - --owner
  - ${SMOKE_GITCODE_OWNER}
format: table
timeout_ms: 8000
assertions:
  - type: exit_code
    value: 0
  - type: stdout_not_empty
  - type: stderr_not_contains
    value: "ERROR"
